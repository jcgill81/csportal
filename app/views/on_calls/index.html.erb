<h1>Listing on_calls</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Phone</th>
    <th>Count</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @on_calls.each do |on_call| %>
  <tr>
    <td><%= on_call.name %></td>
    <td><%= on_call.email %></td>
    <td><%= on_call.phone %></td>
    <td><%= on_call.count %></td>
    <td><%= link_to 'Show', on_call %></td>
    <td><%= link_to 'Edit', edit_on_call_path(on_call) %></td>
    <td><%= link_to 'Destroy', on_call, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New On call', new_on_call_path %>
